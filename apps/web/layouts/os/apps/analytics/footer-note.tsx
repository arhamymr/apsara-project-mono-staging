export default function FooterNote() {
  return (
    <div className="text-muted-foreground mt-8 text-center text-xs">
      UI only — traffic-focused. Wire real analytics later (e.g., pageviews,
      sessions, UTM sources).
    </div>
  );
}
