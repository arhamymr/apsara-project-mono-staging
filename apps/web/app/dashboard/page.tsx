'use client';

// Dashboard index page - the layout handles rendering the OS desktop
// This page component exists to satisfy Next.js routing requirements
export default function DashboardPage() {
  return null;
}
