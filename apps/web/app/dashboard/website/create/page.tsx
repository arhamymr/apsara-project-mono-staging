'use client';

// Dashboard website create page - the parent layout handles rendering the OS desktop
// This page component exists to satisfy Next.js routing requirements
export default function WebsiteCreatePage() {
  return null;
}
