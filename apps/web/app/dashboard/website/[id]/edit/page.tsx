'use client';

type Props = {
  params: Promise<{ id: string }>;
};

// Dashboard website edit page - the parent layout handles rendering the OS desktop
// This page component exists to satisfy Next.js routing requirements
export default function WebsiteEditPage(_props: Props) {
  return null;
}
