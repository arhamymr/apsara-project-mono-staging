'use client';

// Dashboard website index page - the parent layout handles rendering the OS desktop
// This page component exists to satisfy Next.js routing requirements
export default function WebsiteIndexPage() {
  return null;
}
